# Convex Setup

This scaffold defines schema and app-side service boundaries.

## Next steps
- Run `npx convex dev` after installing dependencies.
- Replace in-memory `repository` implementation with Convex mutations/queries.
- Wire auth identity mapping to `users` and account tenancy checks.
